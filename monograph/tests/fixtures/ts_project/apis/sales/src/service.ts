import { validateToken } from '../../../shared/auth/src/token';
import { Product } from '../../shared/types/models';
const utils = require('../../../shared/utils/helpers');

export function getSales() {
    return [];
}
